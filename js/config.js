export const API_URL = 'http://localhost:3000/api';
// Set this to true if deploying to GitHub Pages without a live backend
export const IS_DEMO_MODE = window.location.hostname.includes('github.io') || false;
